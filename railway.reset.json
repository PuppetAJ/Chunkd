{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm --filter server build"
  },
  "deploy": {
    "startCommand": "pnpm --filter server seed",
    "restartPolicyType": "NEVER"
  }
}
